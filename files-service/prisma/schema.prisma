generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model File {
  id          String   @id
  path        String   @unique
  original_name String
  size        BigInt
  mime_type   String?
  created_at  DateTime

  @@map("files")
}
