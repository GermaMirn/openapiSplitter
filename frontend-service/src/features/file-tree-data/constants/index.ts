import type { TreeNodeType } from '@/shared/types';

/** Маппинг тип узла */
export const NODE_TYPE_ICON: Record<TreeNodeType, string> = {
  document: 'pi pi-file',
  schema: 'pi pi-file',
  security: 'pi pi-file',
  path: 'pi pi-file',
  folder: 'pi pi-folder',
  file: 'pi pi-file',
};
